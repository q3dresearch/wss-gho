source_id: who.gho.tobacco-mpower-p2-smokefreeplacedetails
status: active
cadence: monthly
schema_id: gho.v1

publisher: World Health Organization
publisher_tier: first_party
destroys_own_history: true   # a revision overwrites the previous value in place
licence: "CC BY-NC-SA 3.0 IGO; see https://www.who.int/about/policies/publishing/copyright"
personal_data: none
storage: git

notes: >-
  Tobacco MPOWER Protect people: P2 smoke-free places, additional characteristics

  Every row carries a `Date` stamped when WHO last loaded the series, not when
  the observation refers to. Republishing restates the whole history and the
  previous values are gone — that is the perishable fact this captures.
  Last republished 2026-02-25 (193 days before first capture);
  13,650 rows at that point.

endpoints:
  - url: "https://ghoapi.azureedge.net/api/TOBACCO_MPOWER_P2_SMOKEFREEPLACEDETAILS?$top=1000"
    delay_seconds: 2
    timeout_seconds: 120
  - url: "https://ghoapi.azureedge.net/api/TOBACCO_MPOWER_P2_SMOKEFREEPLACEDETAILS?$top=1000&$skip=1000"
    delay_seconds: 2
    timeout_seconds: 120
  - url: "https://ghoapi.azureedge.net/api/TOBACCO_MPOWER_P2_SMOKEFREEPLACEDETAILS?$top=1000&$skip=2000"
    delay_seconds: 2
    timeout_seconds: 120
  - url: "https://ghoapi.azureedge.net/api/TOBACCO_MPOWER_P2_SMOKEFREEPLACEDETAILS?$top=1000&$skip=3000"
    delay_seconds: 2
    timeout_seconds: 120
  - url: "https://ghoapi.azureedge.net/api/TOBACCO_MPOWER_P2_SMOKEFREEPLACEDETAILS?$top=1000&$skip=4000"
    delay_seconds: 2
    timeout_seconds: 120
  - url: "https://ghoapi.azureedge.net/api/TOBACCO_MPOWER_P2_SMOKEFREEPLACEDETAILS?$top=1000&$skip=5000"
    delay_seconds: 2
    timeout_seconds: 120
  - url: "https://ghoapi.azureedge.net/api/TOBACCO_MPOWER_P2_SMOKEFREEPLACEDETAILS?$top=1000&$skip=6000"
    delay_seconds: 2
    timeout_seconds: 120
  - url: "https://ghoapi.azureedge.net/api/TOBACCO_MPOWER_P2_SMOKEFREEPLACEDETAILS?$top=1000&$skip=7000"
    delay_seconds: 2
    timeout_seconds: 120
  - url: "https://ghoapi.azureedge.net/api/TOBACCO_MPOWER_P2_SMOKEFREEPLACEDETAILS?$top=1000&$skip=8000"
    delay_seconds: 2
    timeout_seconds: 120
  - url: "https://ghoapi.azureedge.net/api/TOBACCO_MPOWER_P2_SMOKEFREEPLACEDETAILS?$top=1000&$skip=9000"
    delay_seconds: 2
    timeout_seconds: 120
  - url: "https://ghoapi.azureedge.net/api/TOBACCO_MPOWER_P2_SMOKEFREEPLACEDETAILS?$top=1000&$skip=10000"
    delay_seconds: 2
    timeout_seconds: 120
  - url: "https://ghoapi.azureedge.net/api/TOBACCO_MPOWER_P2_SMOKEFREEPLACEDETAILS?$top=1000&$skip=11000"
    delay_seconds: 2
    timeout_seconds: 120
  - url: "https://ghoapi.azureedge.net/api/TOBACCO_MPOWER_P2_SMOKEFREEPLACEDETAILS?$top=1000&$skip=12000"
    delay_seconds: 2
    timeout_seconds: 120
  - url: "https://ghoapi.azureedge.net/api/TOBACCO_MPOWER_P2_SMOKEFREEPLACEDETAILS?$top=1000&$skip=13000"
    delay_seconds: 2
    timeout_seconds: 120

gates:
  expect_status: 200
  min_bytes: 400
  content_type_any: [json]
  must_contain: ["IndicatorCode"]
  must_not_contain: ["\"error\""]
  # GHO restates whole series, so legitimate month-to-month movement is large.
  # 50% matches the fleet convention: high enough not to fire on a real change,
  # low enough to catch a truncated or empty response.
  max_shrink_pct: 50
